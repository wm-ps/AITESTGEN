[project]
name = "secrets-client"
version = "0.1.0"
description = "SecretsClient port (architecture AD-5) — interface only; Vault/CloudKMS implementations land in Story 1.3"
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/secrets_client"]
