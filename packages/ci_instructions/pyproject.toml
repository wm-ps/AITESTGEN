[project]
name = "ci-instructions"
version = "0.1.0"
description = "CIInstructionsGenerator port — retained as a forward-compatible seam; feature removed 2026-07-15, no templates built"
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ci_instructions"]
