[project]
name = "delivery-adapters"
version = "0.1.0"
description = "DeliveryAdapter port — retained as a forward-compatible seam (AD-4); CI/CD delivery feature removed 2026-07-15, no implementations built"
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/delivery_adapters"]
