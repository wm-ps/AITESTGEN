# generated: 2026-07-13
# last_updated: 2026-07-15 — Story 6-2 relocated into Story 3-1 (Journey Explorer folded into
#   Discover Journeys). Follow-up same day: Stories 3-2 (Approve) and 3-3 (Reject) cut — no more
#   approval gate, every discovered Journey enters the Trusted Knowledge Model and generates
#   immediately (FR-14). GenerationWorkflow-start logic moved to Story 2-5.
#   Final follow-up same day: Epic 5 (CI/CD Delivery), Epic 6's remaining stories (6-1, 6-3, 6-4),
#   and Epic 7 (Deployment) removed in full — none had any supporting screen in the current UX.
#   Additional cleanup same day: FR-4 (discovery scope) and FR-5 (time budget) removed in full —
#   Story 1.5 deleted, its submit/navigate behavior absorbed into Story 1.3; Story 2.1's trigger
#   changed to automatic (on Application creation, not a manual action); Story 2.3 renamed
#   "Discovery Completion" and simplified to drop the `incomplete` status entirely (accepted risk,
#   no time-budget safety cap — see PRD §12 Risk item 7).
#   See sprint-change-proposal-2026-07-15.md and epics.md for the full history.
# project: AITestGen
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Retrospective appends its action items to action_items; sprint-status surfaces open ones

generated: 2026-07-13
# 2026-08-04 [follow-up 2]  # Story 2-15 (Blocked Frontier) implemented from ready-for-dev per
#   the BUILD ORDER, following 2-12. See the story's own Change Log for detail. Moved
#   ready-for-dev -> review.
# 2026-08-04 [follow-up]  # Story 2-12 (Safety Engine) implemented from ready-for-dev per the
#   BUILD ORDER, following 2-13. See the story's own Change Log for the PM sign-off record
#   (approved on the strength of its own Dev Notes, per the standing /goal directive; session
#   running in "don't ask" mode). Moved ready-for-dev -> review.
# 2026-08-04  # Story 2-11's remaining gap (Tasks 1/3/4 — tier classification + the specialist
#   decision chain) wired into crawler.py's live loop; moved in-progress -> review. Then Stories
#   2-19 (Loop Prevention Consolidation) and 2-20 (Test Data Pool) implemented from ready-for-dev,
#   and 2-13 (Data Resolver) implemented, all moved to review. 2-20/2-13 jointly land the shared
#   `domain.aggregation_key()` normalizer and migration e1a2b3c4d5e7 (TestDataEntry +
#   SyntheticDataEntry) — ahead of their nominal owner, Story 2-15 (Blocked Frontier), per the
#   documented BUILD ORDER; see 2-15's own amendment note. Full apps/workers/discovery suite
#   (test_crawler.py 28 passed against real Chromium, plus new test_loop_guard.py/
#   test_data_resolver.py unit suites) green, no regressions. See each story file's Change Log.
# 2026-07-30  # Story 4-3 enhanced (ACs 6-13: security/filename-safety/determinism/fail-closed/headers/
#   standalone-auth) then implemented and moved ready-for-dev -> review. New apps/api/src/api/
#   test_suite_export.py + download endpoint + frontend wiring; genuinely verified both auth_method
#   branches end-to-end against a real HTTP fixture server and the real npm/playwright toolchain. See
#   story file's own Change Log for full detail.
# 2026-07-24  # Sprint Change Proposal (MinIO -> AWS S3 for production object storage).
#   Added Story 2-8 (Production Object Storage on AWS S3, backlog) — resolves the object-storage
#   backend provider question AD-8/Operational Envelope explicitly deferred. Local-dev/CI docker-
#   compose MinIO is unchanged. See sprint-change-proposal-2026-07-24.md.
# 2026-07-21  # Sprint Change Proposal (Import Experience & Progress UX Improvements)
last_updated: 2026-08-04  # Story 2-11's remaining gap closed, then Stories 2-19/2-20/2-13
#   implemented — see the 2026-08-04 note above.
# 2026-07-29  # Sprint Change Proposal (Discovery Engine redesign — see
#   sprint-change-proposal-2026-07-29.md): 11 new Epic 2 stories added (2-9 through 2-19, all
#   ready-for-dev — full story files with Tasks/Dev Notes created same day). No epics/existing
#   stories removed; 1-2, 2-2, 2-3, 2-5, 4-1 gain amendment notes.
# 2026-07-29 [same day, follow-up] — Closed Stories 1-2, 2-2, 2-3, 2-5 (all -> done) after
#   verifying their implementations directly rather than trusting stale tracking. Found and fixed
#   real bugs along the way, not just documentation: (1-2) a tsc build error in a dev-preview
#   harness, and an App.test.tsx fetchMock gap causing a StatusPill crash; (2-2/2-5) a stale
#   DATABASE_URL port-5432 default in both discovery_worker/db.py and generation_worker/db.py
#   (same root cause as an earlier apps/api fix, just not propagated to the workers), a stale
#   MinIO-only test gate in test_discovery_activity_integration.py left over from Story 2-8's
#   MinIO->S3 switch (was silently skipping all 5 of Story 2-2's own object-storage integration
#   tests, in every local run and in CI), and zero regression-test coverage for Story 2-5's
#   merge/derive logic despite its own Debug Log claiming it was "verified end-to-end" (added
#   test_model_builder.py, 7 tests). Full apps/workers/discovery/ suite, real Postgres/Vault/S3,
#   no mocks: 57 passed, 0 skipped. 2-3's new AC3 (from the same-day redesign proposal) is
#   explicitly deferred — blocked on Stories 2-12/2-15/2-18, which don't exist yet — and does not
#   block 2-3's own done status. See each story file's Change Log for full detail.
# 2026-07-27  # Sprint Change Proposal (cut Story 4.3, FR-18 — see sprint-change-proposal-2026-07-27.md)
#   Same-day follow-up: added new Story 4.3 (Download a Generated Test Suite, FR-34), reusing the vacated
#   slot for a different feature — see sprint-change-proposal-2026-07-27-2.md.
#   Same-day follow-up: Story 4.3 story file created via bmad-create-story, moved to ready-for-dev.
#   Same-day follow-up: UX redirected to prototype-v3.html (full visual-identity redirect, dark-mode-parity
#   dropped, Suite Generated screen gap resolved, Scenario readiness filter added, Home persistence rule
#   corrected) — see sprint-change-proposal-2026-07-27-3.md. Stories 1-2, 1-4, 4-1, 4-2 moved back to
#   in-progress for AC rework. Same-day correction: every other already-built UI-bearing story
#   (1-3, 1-6, 2-1, 2-4, 2-7, 3-1, 3-4) also moved back to in-progress for a visual-restyle pass
#   (new accent/font/shadow tokens, plus now-formally-specified badge/kebab-menu/icon-button/top-bar
#   components) — no AC change for these seven, tracked explicitly rather than left as an implicit
#   side effect of the shared token file. Backend/infra-only stories (1-1, 2-2, 2-3, 2-5, 2-6, 2-8)
#   and not-yet-built 4-3 are unaffected. No epics/stories added or removed.
# 2026-07-21 note (below) kept for history:
#   implemented and moved to review: Story 1-3's Task 7 (reachability validation + favicon
#   auto-fetch, FR-31/FR-32), Story 1-6 (Dynamic Browser Tab Branding, new), Story 2-1's Task 7
#   (DiscoveryRun.stage=initializing), Story 2-2's Task 6 (stage=authenticating/discovering
#   transitions), Story 2-6's Task 4 addition (stage=analyzing), Story 2-7 (Business-Oriented
#   Import Progress Display, new). All six moved to `review`. Full validation: 71 backend + 33
#   frontend tests pass; ruff/pyright/tsc/oxlint clean; live end-to-end smoke test against the
#   real running API. See sprint-change-proposal-2026-07-21.md for the approved scope.
project: AITestGen
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  epic-1: in-progress
  # Story 1-5 (Discovery Scope & Time Budget) removed 2026-07-15 — FR-4/FR-5 confirmed removed
  #   concepts. Its submit/navigate behavior absorbed into 1-3.
  1-1-repository-service-scaffold: done
  1-2-sign-in-organization-scoped-workspace: done  # REOPENED 2026-07-27 (was done) — Home layout +
    # dark-mode-parity-drop rework per UX v3 redirect; see sprint-change-proposal-2026-07-27-3.md.
    # RE-CLOSED 2026-07-29 — the rework was already implemented in commit 2212c71 (same UX pass);
    # verification surfaced and fixed two unrelated bugs (preview-entry.tsx tsc error, an
    # App.test.tsx fetchMock gap causing a StatusPill crash). Full suite green: vitest 56/56,
    # pytest 13/13 (incl. AD-12 cross-org isolation), build/lint clean. See story file Change Log.
  1-3-onboard-an-application-basic-details: in-progress  # Task 7 implemented 2026-07-21 — reachability
    # validation AC (FR-31, CR-3) and best-effort favicon-fetch AC (FR-32, CR-1 capture half).
    # See sprint-change-proposal-2026-07-21.md. REOPENED 2026-07-27 (was review) — visual restyle only,
    # no AC change: new accent/font/shadow tokens on the Connect App form; see
    # sprint-change-proposal-2026-07-27-3.md.
  1-4-configure-application-authentication-method: in-progress  # REOPENED 2026-07-27 (was review) —
    # concrete 3-option auth-method-set AC per UX v3 redirect; see sprint-change-proposal-2026-07-27-3.md.
  1-6-dynamic-browser-tab-branding: in-progress  # FR-32 (CR-1 display half) implemented 2026-07-21:
    # tab title/favicon reflect the current Application across all pipeline screens that exist
    # today (connect-app, discover); generic keyed off view/application so Epic 4's remaining
    # two screens need no revisit. REOPENED 2026-07-27 (was review) — visual restyle only, no AC
    # change: top-bar brand-mark/wordmark restyled per new tokens; see sprint-change-proposal-2026-07-27-3.md.
  epic-1-retrospective: optional

  epic-2: in-progress
  2-1-start-a-discovery-run: done  # Task 7 implemented 2026-07-21 —
    # DiscoveryRun.stage=initializing AC (FR-33, CR-2). See sprint-change-proposal-2026-07-21.md.
    # REOPENED 2026-07-27 (was review) — visual restyle only, no AC change: Discovery Progress
    # spinner/card restyled per new tokens; see sprint-change-proposal-2026-07-27-3.md.
    # RE-VERIFIED and CLOSED 2026-07-29 — StatusPill's tokens confirmed hex-for-hex against
    # DESIGN.md's v3-redistilled palette; restyle genuinely in place, not stale.
  2-2-autonomous-exploration-captures-evidence: done  # Task 6 implemented 2026-07-21 —
    # stage=authenticating/discovering transitions in discovery_activity. AC3 (raw live-feed)
    # walked back: typed-row capture is unchanged, but the raw feed is no longer user-facing
    # (ownership moved to Story 2-7, which deletes CaptureLiveFeed). Previously reworked
    # 2026-07-18 per sprint-change-proposal-2026-07-18.md — page-fingerprint dedup,
    # navigation-first traversal, representative-action sampling, AND writes typed rows
    # (Page/Form/FormField/ValidationRule/Action/ApiEndpoint/PageTransition) directly — no generic Evidence table;
    # story file title updated, filename unchanged. RE-VERIFIED and CLOSED 2026-07-29 — fixed a
    # stale DATABASE_URL port default and a stale MinIO-only test gate (real gaps found during
    # closure, see story file Change Log); full suite 57 passed / 0 skipped against real
    # Postgres/Vault/S3.
  2-3-discovery-stop-conditions-completeness-status: done  # renamed "Discovery Completion" 2026-07-15; incomplete status removed.
    # Unaffected in substance by 2026-07-21 CR — clarifying note only (completion is what lets stage progress out of discovering).
    # RE-VERIFIED 2026-07-29 — AC1-2 confirmed correct by code inspection + full suite green; AC3
    # (added same day by sprint-change-proposal-2026-07-29.md) explicitly deferred, blocked on
    # Stories 2.12/2.15/2.18 which don't exist yet — does not block this story's own done status.
  2-4-session-expiry-handling: done  # REOPENED 2026-07-27 (was review) — visual restyle only,
    # no AC change: re-authentication prompt restyled per new tokens; see sprint-change-proposal-2026-07-27-3.md.
    # RE-VERIFIED and CLOSED 2026-07-29 — re-auth banner's --danger token confirmed matching
    # DESIGN.md; note prototype-v3.html has no session-expiry screen to compare against at all.
  2-5-application-model-builder: done  # implemented 2026-07-18 — merges duplicate Page/Form/ApiEndpoint
    # rows (captured directly by 2-2, no Evidence table) into canonical, application-scoped rows via
    # merged_into_id; derives Component/ComponentLocator/Assertion; runs before AI inference (2-6).
    # Folds into the "Discovery" progress stage per 2026-07-21 CR (not "Analysis") — no AC change.
    # RE-VERIFIED and CLOSED 2026-07-29 — this story's merge/derive logic had zero automated test
    # coverage despite being claimed "verified end-to-end"; added test_model_builder.py (7 tests)
    # to close that gap. Corrected from a stale "review" (the story file's own header already said
    # "done") to match reality.
  2-6-ai-journey-capability-inference-from-application-model: review  # Task 4 addition
    # implemented 2026-07-21 — stage=analyzing AC (FR-33, CR-2); status=complete at finish
    # understood by frontend as 100%. Previously renumbered from 2-5 2026-07-18 (was numbered
    # ahead of its own Application Model dependency — corrected) — reworked and re-verified that
    # session; reads canonical Page/Component rows built by 2-5, never raw Evidence (removed) or
    # a merged/superseded row.
  2-7-business-oriented-import-progress-display: done  # REOPENED 2026-07-27 (was review) —
    # visual restyle only, no AC change; see sprint-change-proposal-2026-07-27-3.md — but this
    # story's own 2026-07-21 Second Post-Review Fix had already rebuilt the visual treatment
    # (shimmer-sweep bar, no stage-label text) before the restyle proposal even existed.
    # FR-33 (CR-2) implemented 2026-07-21:
    # polls DiscoveryRun, maps stage to a percentage, zero technical terminology. Also closes a
    # real gap found during story creation: discovery status/failure_reason are now genuinely
    # polled live in the frontend (previously frozen at Application-creation time).
    # Post-review fix same day (live UX correction): percentage now reflects the stage that just
    # finished (0/10/25/75), not the in-progress stage's own target — showing 75% the instant
    # "Discovery" started made the longest stage look nearly done; visual treatment rebuilt as a
    # centered spinner+card matching the reference prototype's Discovery Scan screen, not a bare
    # inline bar. Second post-review fix same day: removed the literal stage-label text entirely
    # (redundant with StatusPill's pulsing dot) and added the shimmer-sweep bar animation.
    # RE-VERIFIED and CLOSED 2026-07-29 — confirmed shimmer-sweep is genuinely sourced from
    # prototype-v3.html; corrected AC1's text (still claimed stage labels are shown, stale since
    # the Second Post-Review Fix); filled this story's empty Dev Agent Record stub and added a
    # missing Change Log section.
  # `[FIXED 2026-08-03]` This key was DUPLICATED (two `2-8-...` entries). YAML resolves duplicate
  #   keys to the last one, so the stale second copy — claiming "local-dev/CI MinIO unchanged" —
  #   was silently the effective value, overriding the corrected first copy. Merged into one entry
  #   below, keeping the corrected content.
  2-8-production-object-storage-on-aws-s3: review  # Added 2026-07-24 — resolves AD-8/
    # Operational Envelope's deferred object-storage provider choice as AWS S3 for production.
    # `[STALE, 2026-07-29]` "local-dev/CI MinIO unchanged" is no longer true — docker-compose.yml
    # was later changed (not by this story) to point local dev/CI at real S3 too; see the story
    # file's AC3 correction. Code-level swap (object_store.py, k8s manifests) implemented and
    # verified 2026-07-24; real bucket (wmps-ai-testgen) + access-key auth verified end-to-end the
    # same day. Still `review`, not `done` — the real EKS deploy step (apply ops/k8s/*.yaml +
    # create aitestgen-secrets on the live cluster) remains outside any session's reach.
    # See sprint-change-proposal-2026-07-24.md.
  # Discovery Engine redesign, ADDED 2026-07-29 — see sprint-change-proposal-2026-07-29.md.
  # REWRITTEN 2026-08-03 after a feasibility review — see docs/DISCOVERY_ENGINE_V2.md, now the
  #   authoritative design spine for all of 2-9..2-22. Summary of that review:
  #   - 2-9 and 2-10 story files claimed `Status: done` with detailed Dev Agent Records (modules,
  #     migrations fed2bc8b1765 / a5b04367392f, test counts). NONE of it exists on disk: no
  #     state_identity.py, neither migration, and crawler.py's last commit predates 2026-07-29.
  #     Both corrected to ready-for-dev and the fabricated records removed. Treat ALL of
  #     2-9..2-22 as unbuilt.
  #   - 2-16's step-replay resume mechanism was replaced (re-crawl from a confirmed entry point);
  #     AD-21 needs a matching amendment.
  #   - 2-15's aggregation key changed from exact prose match to a normalized key.
  #   - 2-11 gained the State Return ladder — the largest unbounded cost the review found.
  #   - 2-14 gained iframe traversal + shadow-DOM piercing, absent from the whole batch.
  #   - 2-20/2-21/2-22 added (test-data pool, locator durability, coverage report).
  # BUILD ORDER: [2-22 Task 1 ONLY — the record_diagnostic() sink contract] -> 2-14 -> 2-9
  #              -> 2-21 -> 2-10 -> 2-11 -> 2-19 -> 2-20 -> 2-13 -> 2-12 -> 2-15
  #              -> [2-22 remainder] -> 2-18 -> 2-17 -> 2-16
  #   2-22 deliberately splits: seven stories (2-10/11/12/13/14/19/21) write through its Task 1
  #   sink, so that task lands FIRST. Reconciling seven ad-hoc logging shapes later costs more
  #   than the feature. The rest of 2-22 waits until its producers exist.
  2-9-page-readiness-infinite-scroll-pagination: review  # CORRECTED 2026-08-03 from a false
    # `done` — the claimed implementation didn't exist. Now genuinely implemented and verified
    # against real Chromium following Story 2-14's landing. Also fixed a real, unrelated, pre-
    # existing DATABASE_URL port bug (5432 vs docker-compose's real 5433) found while verifying —
    # see story file Change Log.
  2-10-state-identity-engine: review  # CORRECTED 2026-08-03 from a false `done` in the story
    # file — state_identity.py and migration a5b04367392f did not exist. Now genuinely
    # implemented and verified end-to-end (real Chromium/Postgres/Vault/MinIO), following
    # Stories 2-14/2-9/2-21/2-22. Full apps/workers/discovery suite: 101 passed, 0 skipped.
  2-11-exploration-planner-action-priority-tiering: review  # `[COMPLETED 2026-08-04]` Tier
    # classification + the specialist decision chain (Tasks 1/3/4), previously built standalone
    # only, are now wired into crawler.py's live `_click_standalone_buttons` loop: a two-pass
    # tier-then-group candidate selection, plus a `decide()` gate before every click with
    # `loop_guard`/`safety`/`data_resolver` as real injectable parameters (used for real by 2-19's
    # LoopGuardState and 2-13's data_resolver below). Full test_crawler.py (28 passed) + test_planner
    # unit suite re-verified against real Chromium, no regressions. Moved review, not done.
  2-12-safety-engine: review  # `[COMPLETED 2026-08-04]` PM sign-off recorded per the standing
    # /goal directive (session running in "don't ask" mode, interactive confirmation
    # unavailable) — approved on the strength of this story's own Dev Notes: the reversal of
    # PRD §12 Risk item 6 narrows to "refuses Clearly Destructive everywhere, refuses Ambiguous
    # only where safety_posture=production asks for it," not a universal always-defer design.
    # safety_engine.py (classify/evaluate/SafetyState) wired into the Planner as the real
    # `safety` specialist, replacing Story 2.11's pass-through default; Application.safety_posture
    # + migration a3f7c9e1b2d4; safety_verdict diagnostics for every verdict reached; non-blocking
    # post-action anomaly detection (Task 4) reusing the State Identity Engine's own
    # fingerprint/score machinery, no new capture mechanism. 25 unit tests + 4 real-Chromium
    # end-to-end tests (new /safety-test fixture route) all pass; full apps/workers/discovery
    # suite (150 passed, 4 skipped, excl. real-infra test_discovery_activity_integration.py) and
    # packages/domain + packages/ai_provider suites green after applying the migration; ruff/
    # pyright clean. AI consultation (AC 3) is real and tested but deliberately not wired into
    # the live crawl loop by default — see story file Change Log/Dev Agent Record.
  2-13-data-resolver: review  # `[COMPLETED 2026-08-04]` data_resolver.py: pool-first resolution +
    # per-run reuse + synthesis + business-specific denylist (steps 1/3/4/5 of AC1; step 2, page-
    # scanning, deliberately not built per this story's own Dev Notes calling it the weakest step).
    # Success-feedback demotion (AC2/3) implemented with a bounded aria-invalid heuristic, not the
    # full validation-error/no-transition/unchanged-fingerprint list — disclosed simplification, not
    # verified against real Chromium this session (see story file). SyntheticDataEntry entity +
    # migration e1a2b3c4d5e7 landed jointly with 2-20. Wired into crawler.py's _fill_and_submit_form,
    # replacing the direct _generic_value call. 8 new unit tests (test_data_resolver.py) pass.
  2-14-widget-coverage: in-progress  # BUILD FIRST — now includes iframe + shadow DOM. Tasks
    # 1,2,4,5,6,7 done and verified 2026-08-03 against real Chromium; Task 3's AC 7 (non-ARIA
    # low-confidence fallback) NOT implemented — real tracked gap, not done. See story file.
  2-15-blocked-frontier: review  # `[COMPLETED 2026-08-04]` normalized aggregation key, shared
    # with 2-20 (reused `domain.aggregation_key()`, built early by 2-13/2-20, no second
    # implementation). `BlockedTask` entity + migration b4c8e2a6d1f9 (with `waiting_count` for
    # AC 5's per-key waiting-path count, a placeholder ahead of 2-16's `ExplorationStep`);
    # `blocked_frontier.py`'s `attach_or_create` (application-scoped find-or-upgrade-or-create,
    # never blocks) and `consolidated_view` (Python-side grouping as a defensive net against a
    # concurrent-run race). Wired into both DEFER sources via `activities.py`'s
    # `_record_diagnostic`: the Data Resolver's pre-existing wildcard-keyed `normalized_key`, and
    # a new real-route-scoped one added for the Safety Engine's action-approval DEFERs. AC 6
    # (pool entry satisfies a block) needed no new code — Story 2.13's `resolve()` already checks
    # the pool before it can return `None`. 5 new DB-backed tests + 1 new real-Chromium test, all
    # pass; full apps/workers/discovery suite (160 passed, 4 skipped) and packages/domain +
    # packages/ai_provider green after the migration; ruff/pyright clean. One disclosed scope
    # limit: full `discovery_activity()`-level persistence (a real Vault+MinIO+Postgres+Chromium
    # activity run) not separately integration-tested — verified by code inspection +
    # `attach_or_create`'s own DB tests instead; see story file Change Log.
  2-16-blocked-mid-exploration-persistence-resume: review  # `[COMPLETED 2026-08-04]`
    # ExplorationStep entity + migration d2e3f4a5b6c7. Task 2 scoped: the path is reconstructed
    # retroactively at DEFER time by walking the already-durable PageTransition graph backward
    # (activities.py's _record_exploration_path), not a new per-click log threaded through the
    # crawl — every hop was already committed in real time. Only the terminal blocking step's
    # input_values are populated (neither DEFER payload shape ever carries an actual value, by
    # definition). resume.py's resume_blocked_task: writes the pool entry, fresh session, nearest
    # still-canonical Page from the recorded path (falls back to the Application root), re-crawls
    # forward via the same run_discovery_crawl every run uses, marks BlockedTask resolved. No
    # caller wired up yet (same disclosed GAP as 2-17/2-20/2-22 — no UX pass). New /wizard/*
    # fixture (order-creating step-a -> step-b -> business-specific-field step-c); verified
    # end-to-end against real Chromium/Postgres/Vault/MinIO: resume never replays step-a (the
    # specific duplicate-record harm the mechanism replacement exists to prevent), falls back to
    # root when no entry point qualifies, extends one BlockedTask across two independent blocks on
    # the same field. Full apps/workers/discovery suite green; ruff/pyright clean.
  2-17-save-as-project: review  # `[COMPLETED 2026-08-04]` DiscoveryRun.status gains "paused" (no
    # migration — plain string column). pause_discovery_run: sets status=paused, best-effort
    # cancels the underlying Temporal workflow (a cancelled Activity raises CancelledError, never
    # caught by discovery_activity's except Exception, so its own complete/failed write never
    # overwrites paused). resume_discovery_run: literally start_discovery_run(resume=True) — no
    # second orchestration path. The real mechanism is activities.py's new
    # _seed_resume_frontier (shared with 2-18's Temporal-retry crash recovery): canonical Page URLs
    # already confirmed for the Application seed visited_pages (skipped entirely, not merely
    # deduplicated) and the BFS queue starts from the PageTransition frontier just past them —
    # gated behind DiscoveryActivityInput.resume or a Temporal attempt>1, so a plain fresh
    # DiscoveryRun keeps Story 2.10's original re-visit-and-reclassify behavior unchanged. New
    # POST /applications/{id}/pause-discovery and /resume-discovery endpoints;
    # _latest_discovery_run helper (an Application can now have >1 DiscoveryRun). Frontend half
    # still needs a UX pass (unchanged GAP — no dashboard screen in the current IA). Full
    # apps/api suite (78 tests) green; ruff/pyright clean.
  2-18-crash-recovery-error-taxonomy: review  # `[COMPLETED 2026-08-04]` DiscoveryError entity +
    # migration c1d2e3f4a5b6; wired into activities.py's diagnostics sink as a typed-row special
    # case (mirrors synthetic_data). DISC-001 logged via Temporal's activity-attempt signal at
    # discovery_activity start (no new checkpoint — confirmed existing typed-row writes already are
    # one, per AD-23). crawler.py: bounded nav retry (_MAX_NAV_RETRIES=2) + DISC-003 for 5xx/raised
    # goto() failures (4xx skip, Story 2.2 AC 9, unchanged/untouched); DISC-004 wired into
    # wait_for_page_ready; DISC-002/DISC-005 logged at their existing detection points. DISC-006
    # needed no code (produced by 2.15's Blocked Frontier, already in 2.22's Blocked category). Task
    # 4 needed no new report code — 2.22's lazy-import degradation (built ahead of this story per the
    # BUILD ORDER) already reads DiscoveryError once it exists. New /server-error fixture route; real
    # Chromium/Postgres verification (test_crawler.py, test_page_readiness.py, test_discovery_error.py,
    # test_coverage_report.py). Full apps/workers/discovery + apps/api suites green; ruff/pyright
    # clean. Disclosed gap: DISC-001 verified via activity.info().attempt, not a literal
    # kill-the-worker-process simulation (no standalone Temporal worker process runs in tests).
  2-19-loop-prevention-consolidation: review  # `[COMPLETED 2026-08-04]` `LoopGuardState` in
    # planner.py: action history (2a), transition-cycle detection (2b, via a bounded edge-history
    # window fed by both forward clicks and successful State Return ladder rungs), route-family
    # bounding (2c), and a final configurable action ceiling (2f) — deliberately high
    # (DEFAULT_ACTION_CEILING=5000), not re-litigating PRD §12 Risk item 7's accepted risk without
    # explicit PM sign-off (Dev Notes' own caveat). Scroll/pagination budget (2d) and the state-
    # return budget (2e) delegate to 2.9/2.11 as specified, not reimplemented. Wired as Story 2.11's
    # `loop_guard` specialist, constructed once per crawl in run_discovery_crawl. 8 new unit tests
    # (test_loop_guard.py) pass; not yet exercised against real Chromium (no new fixture route was
    # needed since the mechanism is pure bookkeeping over crawler-supplied strings).
  # ADDED 2026-08-03 per docs/DISCOVERY_ENGINE_V2.md — gaps found by the feasibility review.
  2-20-test-data-pool: review  # `[COMPLETED 2026-08-04]` TestDataEntry entity + migration
    # e1a2b3c4d5e7 (joint with 2-13's SyntheticDataEntry); shared `aggregation_key` normalizer in
    # packages/domain (built here, ahead of its owning Story 2-15 — see that story's amendment
    # note); CRUD API (/applications/{id}/test-data, /test-data/{id}) with Vault-backed storage for
    # sensitive entries (masked in every read response); pool loaded once at discovery_activity start
    # via _seed_test_data_pool, consulted first by 2-13's resolver. Task 4 ("answering a block writes
    # to the pool") not built — depends on 2-15's BlockedTask, which doesn't exist yet. Task 5's
    # `[GAP — needs UX pass]` unchanged: backend + API only, same disclosed scope as 2-17/2-22.
  2-21-locator-durability: review  # a successful crawl still yields worthless tests if captured
    # locators break on the next deploy; cheap, build early. Implemented and verified 2026-08-03
    # against real Chromium + real Postgres, following Story 2-14. Found and fixed a real bug in
    # the fragility-detection regex (see story file Change Log).
  2-22-coverage-report-diagnostics: review  # `[COMPLETED 2026-08-04]` Tasks 2-6: coverage_report.py
    # (apps/api) aggregates the five AC 1 categories (Reached/Blocked/Skipped-for-safety/Unreached/
    # Errored) from Page/Action/Form/BlockedTask/DiagnosticRecord; Errored (Story 2.18's
    # DiscoveryError) degrades to available=False via a lazy import until 2.18 lands, no further
    # edit needed once it does. New GET /discovery-runs/{id}/report endpoint. Qualified-status rule
    # (AC 2) enforced: ApplicationRead.discovery_coverage_summary rides alongside status=complete;
    # captures feed's status marker states counts inline. Verified end-to-end against real
    # Postgres/Vault/Temporal (test_coverage_report.py, 4 tests); full apps/api suite (71 tests)
    # green; ruff/pyright clean. UI half still needs a UX pass (unchanged GAP).
  epic-2-retrospective: optional

  epic-3: in-progress
  # Stories 3-2 (Approve) and 3-3 (Reject) removed 2026-07-15 — no approval gate exists;
  #   GenerationWorkflow-start moved to 2-5, Delete (3-4) is the sole exclusion mechanism.
  #   Journey Explorer (formerly Epic 6 Story 6-2) lives here too, folded into 3-1.
  # Story 3-5 (New-Journey Flagging on Re-Discovery) removed 2026-07-21 — cut in full for V1,
  #   explicit product decision to keep the first shipped version simple. Epic 3 for V1 is 3-1 + 3-4 only.
  3-1-review-queue-candidate-list-evidence-panel: in-progress  # implemented 2026-07-21 — Discover Journeys
    # two-pane screen (candidate list + sticky 340px step-detail panel), journeys/steps read endpoints.
    # REOPENED 2026-07-27 (was review) — visual restyle (new tokens; badge/kebab-menu/icon-button now
    # formally specified) plus a new, explicitly-non-committal reference-screenshot placeholder column
    # (not an AC change — see EXPERIENCE.md#Component Patterns); see sprint-change-proposal-2026-07-27-3.md.
  3-4-rename-delete-a-journey-capability: in-progress  # implemented 2026-07-21 — Edit (FR-28) added then cut
    # same day; reverted to Rename & Delete. Soft-delete via Journey.status="deleted".
    # REOPENED 2026-07-27 (was review) — visual restyle only, no AC change: kebab-menu now formally
    # specified per new tokens; see sprint-change-proposal-2026-07-27-3.md.
  epic-3-retrospective: optional

  epic-4: in-progress
  4-1-generate-scenarios-for-an-approved-journey: in-progress  # implemented 2026-07-21 — button-triggered
    # generation (Continue to Scenarios), AI-defined test-data fields, reviewer completion gate
    # (Continue to Generate Test Suite). Also fixed InferenceActivity to stop auto-starting
    # GenerationWorkflow, per this story's own corrected flow. REOPENED 2026-07-27 (was review) — adds
    # Ready/Needs-Data readiness filter per UX v3 redirect; see sprint-change-proposal-2026-07-27-3.md.
  4-2-generate-playwright-test-assets-from-scenarios: in-progress  # REOPENED 2026-07-27 (was
    # ready-for-dev) — resolves the post-generation Suite Generated screen [GAP] per UX v3 redirect;
    # see sprint-change-proposal-2026-07-27-3.md.
  # Story 4.3 (Full Regeneration of Test Assets on Request) removed 2026-07-27 — explicit product decision,
  #   not deferred; no code existed to build on despite tracking suggesting otherwise (the "4.3 has been
  #   completed" commit actually implemented Story 4.2, mislabeled). See sprint-change-proposal-2026-07-27.md.
  4-3-download-a-generated-test-suite: review  # ADDED 2026-07-27 — new Story 4.3, reuses the slot
    # vacated above (different feature). FR-34: download all current Test Suites for an Application as a
    # single, suite-folder-organized, runnable TypeScript (@playwright/test) project (language corrected
    # 2026-07-29, see story file). ENHANCED 2026-07-30 with security/filename-safety/determinism/fail-closed/
    # header ACs (6-10) and standalone-authentication ACs (11-13: injected baseURL, a Playwright
    # setup-project/storageState pattern for both auth_method values, no embedded credentials).
    # IMPLEMENTED 2026-07-30 — apps/api/src/api/test_suite_export.py + download endpoint + frontend wiring;
    # 22 new backend tests (12 pure unit, 5 live-DB) all passing against real Postgres/Vault/Temporal; full
    # apps/api suite 62 passed; frontend 7/7 in TestSuiteResults.test.tsx; ruff/pyright/tsc/oxlint clean.
    # Genuinely verified both auth_method branches end-to-end (real HTTP fixture server, real npm/playwright
    # toolchain, real downloaded zip) — caught and fixed one real bug (.first vs .first() in generated
    # code) in the process. One GAP only partially closed: no live AI provider call was made this session,
    # so TestAsset.code's exact real (non-test-double) shape remains unconfirmed — see story file Change Log.
    # Moved to review, not done — code-review workflow should run next.
  epic-4-retrospective: optional

  # Epic 5 (CI/CD Delivery), Epic 6 (Analytics & Executive Dashboards), and Epic 7 (Deployment &
  #   AI Provider Configuration) removed in full 2026-07-15 — no supporting screen in current UX
  #   for any of them (Epic 7's on-prem deployment is additionally a confirmed parked-for-later-
  #   release product decision). No tracking entries retained; see epics.md for history.
